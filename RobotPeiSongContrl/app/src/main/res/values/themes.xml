<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RobotPeiSongContrl" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/mint_green</item>
        <item name="colorPrimaryDark">@color/mint_green</item>
        <item name="colorAccent">@color/mint_green</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mint_green</item>
        <item name="colorPrimaryDark">@color/mint_green</item>
        <item name="colorAccent">@color/mint_green</item>
    </style>
    <style name="PasswordButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:background">@drawable/button_password_bg</item>
    </style>
    <style name="BackButtonStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:src">@drawable/ic_arrow_back</item>
        <item name="android:background">?selectableItemBackgroundBorderless</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="Theme.RobotPeiSongContrl" parent="Base.Theme.RobotPeiSongContrl" />
</resources>